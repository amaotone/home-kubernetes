# BitwardenSecret for Cloudflare Tunnel
# This file is a template. Copy to cloudflare-bitwarden-secret.yaml and fill in actual values.
#
# Steps:
# 1. Create secret in Bitwarden Secrets Manager:
#    - cloudflare-tunnel-token (value: your tunnel token from Cloudflare Dashboard)
#
# 2. Get secret ID from Bitwarden (click on the secret in web UI)
#
# 3. Copy this file:
#    cp cloudflare-bitwarden-secret.yaml.example cloudflare-bitwarden-secret.yaml
#
# 4. Replace REPLACE_WITH_* values with actual IDs
#
# 5. Apply:
#    kubectl apply -f cloudflare-bitwarden-secret.yaml

apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: cloudflare-tunnel-token
  namespace: cloudflared
  labels:
    app: cloudflared
spec:
  # Organization ID from Bitwarden
  # Get from: Bitwarden Web Vault → Settings → Organization ID
  organizationId: "REPLACE_WITH_YOUR_ORG_ID"

  # Reference to the operator's access token secret
  authToken:
    secretName: bw-auth-token
    secretNamespace: sm-operator-system

  # Name of the Kubernetes secret to create/update
  secretName: cloudflare-tunnel-token

  # Map Bitwarden secret to Kubernetes secret key
  # Get secret ID from Bitwarden Web UI
  map:
    - bwSecretId: "REPLACE_WITH_CLOUDFLARE_TOKEN_SECRET_ID"
      secretKeyName: token
